<nav class="navbar container">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Alfood Food Reviews</a>
    </div>
    <!--
    <ul class="nav navbar-nav">

    </ul>
    -->
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li><%= current_user.email %></li>
        <li><%= link_to "Edit Account", edit_user_registration_path, class: 'btn btn-primary' %></li>
        <li><%= link_to "Log Out", destroy_user_session_path, class: 'btn btn-primary'  %></li>
      <% else %>
        <li><%= link_to "Log In", new_user_session_path, class: 'btn btn-primary' %>
        </li>
        <li><%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-default' %></li>
      <% end %>  
    </ul>
  </div>
</nav>